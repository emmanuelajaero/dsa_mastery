window.GAME_DATA = {
"patterns": [
{
"id": "prefix-sum",
"name": "Prefix Sum",
"cat": "core",
"clue": "Many range-sum queries on a fixed array",
"time": "O(n)",
"space": "O(n)",
"rec": "",
"mnemonic": "Pre-cook the sums, serve ranges instantly.",
"confus": [
"sliding-window",
"two-pointers",
"kadane"
],
"template": [
"def build_prefix(nums):",
"    prefix = [0] * (len(nums) + 1)      # prefix[0] = 0 sentinel avoids i-1 edge cases",
"    for i, x in enumerate(nums):",
"        prefix[i + 1] = prefix[i] + x",
"    return prefix",
"def range_sum(prefix, i, j):            # inclusive sum of nums[i..j]",
"    return prefix[j + 1] - prefix[i]"
],
"example": [
"from collections import defaultdict",
"def subarray_sum(nums, k):",
"    count = 0",
"    running = 0",
"    seen = defaultdict(int)",
"    seen[0] = 1                          # empty prefix",
"    for x in nums:",
"        running += x",
"        count += seen[running - k]       # how many earlier prefixes make (running - prev) == k",
"        seen[running] += 1",
"    return count"
]
},
{
"id": "two-pointers",
"name": "Two Pointers",
"cat": "core",
"clue": "Sorted array — find a pair/triplet meeting a condition",
"time": "O(n)",
"space": "O(1)",
"rec": "",
"mnemonic": "Squeeze from both ends.",
"confus": [
"sliding-window",
"binary-search",
"prefix-sum"
],
"template": [
"def two_sum_sorted(nums, target):",
"    left, right = 0, len(nums) - 1",
"    while left < right:",
"        s = nums[left] + nums[right]",
"        if s == target:",
"            return [left, right]",
"        elif s < target:",
"            left += 1                    # need bigger -> raise the floor",
"        else:",
"            right -= 1                   # need smaller -> lower the ceiling",
"    return [-1, -1]"
],
"example": [
"def three_sum(nums):",
"    nums.sort()",
"    res = []",
"    n = len(nums)",
"    for i in range(n - 2):",
"        if i > 0 and nums[i] == nums[i - 1]:",
"            continue                     # skip duplicate anchors",
"        left, right = i + 1, n - 1",
"        while left < right:",
"            total = nums[i] + nums[left] + nums[right]",
"            if total == 0:",
"                res.append([nums[i], nums[left], nums[right]])",
"                left += 1",
"                right -= 1",
"                while left < right and nums[left] == nums[left - 1]:",
"                    left += 1            # skip duplicate seconds",
"                while left < right and nums[right] == nums[right + 1]:",
"                    right -= 1           # skip duplicate thirds",
"            elif total < 0:",
"                left += 1",
"            else:",
"                right -= 1",
"    return res"
]
},
{
"id": "sliding-window",
"name": "Sliding Window",
"cat": "core",
"clue": "Longest / shortest CONTIGUOUS subarray or substring",
"time": "O(n)",
"space": "O(k)",
"rec": "",
"mnemonic": "Grow right, shrink left, keep the best.",
"confus": [
"two-pointers",
"prefix-sum",
"kadane"
],
"template": [
"def max_sum_subarray(nums, k):",
"    window = sum(nums[:k])",
"    best = window",
"    for right in range(k, len(nums)):",
"        window += nums[right] - nums[right - k]   # add new, drop old",
"        best = max(best, window)",
"    return best"
],
"example": null
},
{
"id": "fast-slow",
"name": "Fast & Slow Pointers",
"cat": "core",
"clue": "Detect a cycle in a linked list / find the middle node",
"time": "O(n)",
"space": "O(1)",
"rec": "",
"mnemonic": "Tortoise and Hare — the hare laps the loop.",
"confus": [
"ll-reversal",
"two-pointers",
"binary-search"
],
"template": [
"class ListNode:",
"    def __init__(self, val=0, next=None):",
"        self.val = val",
"        self.next = next",
"def has_cycle(head):",
"    slow = fast = head",
"    while fast and fast.next:",
"        slow = slow.next",
"        fast = fast.next.next",
"        if slow is fast:",
"            return True",
"    return False"
],
"example": [
"def find_duplicate(nums):",
"    slow = fast = nums[0]",
"    while True:                          # phase 1: find collision",
"        slow = nums[slow]",
"        fast = nums[nums[fast]]",
"        if slow == fast:",
"            break",
"    slow = nums[0]                       # phase 2: find entrance",
"    while slow != fast:",
"        slow = nums[slow]",
"        fast = nums[fast]",
"    return slow"
]
},
{
"id": "ll-reversal",
"name": "LinkedList In-place Reversal",
"cat": "core",
"clue": "Reverse a list (or sublist) using O(1) extra space",
"time": "O(n)",
"space": "O(1)",
"rec": "",
"mnemonic": "Three hands: prev, curr, next — flip and march.",
"confus": [
"fast-slow",
"two-pointers",
"tree-traversal"
],
"template": [
"def reverse_list(head):",
"    prev = None",
"    curr = head",
"    while curr:",
"        nxt = curr.next                  # 1) remember the rest",
"        curr.next = prev                 # 2) flip the pointer",
"        prev = curr                      # 3) advance prev",
"        curr = nxt                       # 4) advance curr",
"    return prev"
],
"example": [
"def reverse_between(head, m, n):",
"    dummy = ListNode(0, head)",
"    prev = dummy",
"    for _ in range(m - 1):               # walk to node before position m",
"        prev = prev.next",
"    curr = prev.next",
"    for _ in range(n - m):               # head-insertion reversal of the sublist",
"        nxt = curr.next",
"        curr.next = nxt.next",
"        nxt.next = prev.next",
"        prev.next = nxt",
"    return dummy.next"
]
},
{
"id": "monotonic-stack",
"name": "Monotonic Stack",
"cat": "core",
"clue": "Next greater / next smaller element",
"time": "O(n)",
"space": "O(n)",
"rec": "",
"mnemonic": "Keep the stack tidy; pop the losers when a bigger one arrives.",
"confus": [
"top-k",
"sliding-window",
"binary-search"
],
"template": [
"def next_greater(nums):",
"    n = len(nums)",
"    res = [-1] * n",
"    stack = []                           # holds indices, values decreasing",
"    for i, x in enumerate(nums):",
"        while stack and nums[stack[-1]] < x:",
"            res[stack.pop()] = x         # x is the next greater for popped index",
"        stack.append(i)",
"    return res"
],
"example": [
"def daily_temperatures(temps):",
"    res = [0] * len(temps)",
"    stack = []                           # decreasing temperatures (indices)",
"    for i, t in enumerate(temps):",
"        while stack and temps[stack[-1]] < t:",
"            j = stack.pop()",
"            res[j] = i - j               # distance in days",
"        stack.append(i)",
"    return res"
]
},
{
"id": "top-k",
"name": "Top K Elements (Heap)",
"cat": "core",
"clue": "K largest / smallest / most frequent",
"time": "O(n log k)",
"space": "O(k)",
"rec": "",
"mnemonic": "Keep a heap of size K; kick out the weakest.",
"confus": [
"monotonic-stack",
"binary-search",
"intervals"
],
"template": [
"import heapq",
"def kth_largest(nums, k):",
"    heap = []                            # min-heap of the k largest so far",
"    for x in nums:",
"        heapq.heappush(heap, x)",
"        if len(heap) > k:",
"            heapq.heappop(heap)          # drop the smallest -> keep top k",
"    return heap[0]                       # root = kth largest"
],
"example": [
"from collections import Counter",
"import heapq",
"def top_k_frequent(nums, k):",
"    freq = Counter(nums)",
"    # nlargest by frequency: O(n log k)",
"    return [val for val, _ in heapq.nlargest(k, freq.items(), key=lambda kv: kv[1])]"
]
},
{
"id": "intervals",
"name": "Overlapping Intervals",
"cat": "core",
"clue": "Merge / insert / schedule overlapping ranges",
"time": "O(n log n)",
"space": "O(n)",
"rec": "",
"mnemonic": "Sort by start, then stitch the overlaps.",
"confus": [
"two-pointers",
"binary-search",
"top-k"
],
"template": [
"def merge_intervals(intervals):",
"    intervals.sort(key=lambda iv: iv[0]) # sort by start",
"    merged = []",
"    for start, end in intervals:",
"        if merged and start <= merged[-1][1]:",
"            merged[-1][1] = max(merged[-1][1], end)   # extend the last",
"        else:",
"            merged.append([start, end])               # disjoint -> new block",
"    return merged"
],
"example": [
"def erase_overlap_intervals(intervals):",
"    intervals.sort(key=lambda iv: iv[1]) # sort by END time",
"    removals = 0",
"    prev_end = float(\"-inf\")",
"    for start, end in intervals:",
"        if start >= prev_end:",
"            prev_end = end               # keep it",
"        else:",
"            removals += 1                # overlaps -> drop it",
"    return removals"
]
},
{
"id": "binary-search",
"name": "Modified Binary Search",
"cat": "core",
"clue": "Sorted or rotated array — find target / boundary / peak",
"time": "O(log n)",
"space": "O(1)",
"rec": "",
"mnemonic": "Halve and conquer — even when the array is twisted.",
"confus": [
"two-pointers",
"intervals",
"lis"
],
"template": [
"def binary_search(nums, target):",
"    lo, hi = 0, len(nums) - 1",
"    while lo <= hi:",
"        mid = (lo + hi) // 2             # in Python no overflow, but habit: lo + (hi-lo)//2",
"        if nums[mid] == target:",
"            return mid",
"        elif nums[mid] < target:",
"            lo = mid + 1",
"        else:",
"            hi = mid - 1",
"    return -1"
],
"example": [
"def search_rotated(nums, target):",
"    lo, hi = 0, len(nums) - 1",
"    while lo <= hi:",
"        mid = (lo + hi) // 2",
"        if nums[mid] == target:",
"            return mid",
"        if nums[lo] <= nums[mid]:        # left half is sorted",
"            if nums[lo] <= target < nums[mid]:",
"                hi = mid - 1",
"            else:",
"                lo = mid + 1",
"        else:                            # right half is sorted",
"            if nums[mid] < target <= nums[hi]:",
"                lo = mid + 1",
"            else:",
"                hi = mid - 1",
"    return -1"
]
},
{
"id": "tree-traversal",
"name": "Binary Tree Traversal",
"cat": "core",
"clue": "Pre / In / Post order — sorted output from a BST (inorder)",
"time": "O(n)",
"space": "O(h)",
"rec": "",
"mnemonic": "PIP — **P**re (root first), **I**n (root middle), **P**ost (root last).",
"confus": [
"dfs",
"bfs",
"tree-dp"
],
"template": [
"class TreeNode:",
"    def __init__(self, val=0, left=None, right=None):",
"        self.val = val",
"        self.left = left",
"        self.right = right",
"def inorder(root):",
"    res = []",
"    def dfs(node):",
"        if not node:",
"            return",
"        dfs(node.left)",
"        res.append(node.val)             # visit root between children",
"        dfs(node.right)",
"    dfs(root)",
"    return res"
],
"example": [
"def kth_smallest(root, k):",
"    stack = []",
"    curr = root",
"    while stack or curr:",
"        while curr:                      # dive left",
"            stack.append(curr)",
"            curr = curr.left",
"        curr = stack.pop()",
"        k -= 1",
"        if k == 0:",
"            return curr.val              # kth visited = kth smallest",
"        curr = curr.right                # then go right"
]
},
{
"id": "dfs",
"name": "Depth-First Search (DFS)",
"cat": "core",
"clue": "Enumerate paths / detect cycle / topological sort",
"time": "O(V + E)",
"space": "O(V)",
"rec": "",
"mnemonic": "Go deep, hit a wall, back up, try the next door.",
"confus": [
"bfs",
"backtracking",
"matrix"
],
"template": [
"def dfs(graph, start):",
"    visited = set()",
"    order = []",
"    def explore(node):",
"        visited.add(node)",
"        order.append(node)",
"        for nxt in graph[node]:",
"            if nxt not in visited:",
"                explore(nxt)",
"    explore(start)",
"    return order"
],
"example": [
"def find_order(num_courses, prerequisites):",
"    graph = {i: [] for i in range(num_courses)}",
"    for course, pre in prerequisites:",
"        graph[pre].append(course)        # pre -> course",
"    state = [0] * num_courses            # 0=unseen, 1=visiting, 2=done",
"    order = []",
"    ok = True",
"    def dfs(node):",
"        nonlocal ok",
"        state[node] = 1",
"        for nxt in graph[node]:",
"            if state[nxt] == 1:          # back-edge -> cycle",
"                ok = False",
"                return",
"            if state[nxt] == 0:",
"                dfs(nxt)",
"        state[node] = 2",
"        order.append(node)               # postorder = reverse topo order",
"    for c in range(num_courses):",
"        if state[c] == 0:",
"            dfs(c)",
"    return order[::-1] if ok else []"
]
},
{
"id": "bfs",
"name": "Breadth-First Search (BFS)",
"cat": "core",
"clue": "Fewest steps / shortest path in an UNWEIGHTED graph",
"time": "O(V + E)",
"space": "O(V)",
"rec": "",
"mnemonic": "Ripples in a pond — nearest first, level by level.",
"confus": [
"dfs",
"dijkstra",
"matrix"
],
"template": [
"from collections import deque",
"def bfs_levels(root):",
"    if not root:",
"        return []",
"    res = []",
"    q = deque([root])",
"    while q:",
"        level = []",
"        for _ in range(len(q)):          # snapshot: process exactly this level",
"            node = q.popleft()",
"            level.append(node.val)",
"            if node.left:",
"                q.append(node.left)",
"            if node.right:",
"                q.append(node.right)",
"        res.append(level)",
"    return res"
],
"example": [
"from collections import deque",
"def oranges_rotting(grid):",
"    rows, cols = len(grid), len(grid[0])",
"    q = deque()",
"    fresh = 0",
"    for r in range(rows):",
"        for c in range(cols):",
"            if grid[r][c] == 2:",
"                q.append((r, c))         # all initial sources",
"            elif grid[r][c] == 1:",
"                fresh += 1",
"    minutes = 0",
"    while q and fresh:",
"        minutes += 1",
"        for _ in range(len(q)):          # one minute = one level",
"            r, c = q.popleft()",
"            for dr, dc in ((1,0),(-1,0),(0,1),(0,-1)):",
"                nr, nc = r + dr, c + dc",
"                if 0 <= nr < rows and 0 <= nc < cols and grid[nr][nc] == 1:",
"                    grid[nr][nc] = 2",
"                    fresh -= 1",
"                    q.append((nr, nc))",
"    return -1 if fresh else minutes"
]
},
{
"id": "matrix",
"name": "Matrix Traversal",
"cat": "core",
"clue": "Islands / flood fill / regions on a grid",
"time": "O(m·n)",
"space": "O(m·n)",
"rec": "",
"mnemonic": "A grid is just a graph — each cell has 4 neighbors.",
"confus": [
"dfs",
"bfs",
"grid-dp"
],
"template": [
"def flood_fill(image, sr, sc, new_color):",
"    rows, cols = len(image), len(image[0])",
"    start = image[sr][sc]",
"    if start == new_color:",
"        return image                     # avoid infinite recursion",
"    def dfs(r, c):",
"        if 0 <= r < rows and 0 <= c < cols and image[r][c] == start:",
"            image[r][c] = new_color",
"            for dr, dc in ((1,0),(-1,0),(0,1),(0,-1)):",
"                dfs(r + dr, c + dc)",
"    dfs(sr, sc)",
"    return image"
],
"example": [
"def num_islands(grid):",
"    if not grid:",
"        return 0",
"    rows, cols = len(grid), len(grid[0])",
"    count = 0",
"    def sink(r, c):",
"        if 0 <= r < rows and 0 <= c < cols and grid[r][c] == \"1\":",
"            grid[r][c] = \"0\"             # mark visited by sinking",
"            sink(r + 1, c); sink(r - 1, c)",
"            sink(r, c + 1); sink(r, c - 1)",
"    for r in range(rows):",
"        for c in range(cols):",
"            if grid[r][c] == \"1\":",
"                count += 1",
"                sink(r, c)               # remove the whole island",
"    return count"
]
},
{
"id": "backtracking",
"name": "Backtracking",
"cat": "core",
"clue": "Generate ALL permutations / combinations / subsets",
"time": "O(branch^depth)",
"space": "O(depth)",
"rec": "",
"mnemonic": "Choose → Explore → Un-choose.",
"confus": [
"dfs",
"dp-bridge",
"bitmask-dp"
],
"template": [
"def backtrack(choices):",
"    res = []",
"    path = []",
"    def explore(remaining):",
"        if not remaining:                # base case: a complete solution",
"            res.append(path[:])          # copy the path",
"            return",
"        for i, choice in enumerate(remaining):",
"            path.append(choice)                          # choose",
"            explore(remaining[:i] + remaining[i+1:])     # explore",
"            path.pop()                                   # un-choose",
"    explore(choices)",
"    return res"
],
"example": [
"def subsets(nums):",
"    res = []",
"    path = []",
"    def explore(start):",
"        res.append(path[:])              # every node is a valid subset",
"        for i in range(start, len(nums)):",
"            path.append(nums[i])         # choose nums[i]",
"            explore(i + 1)               # explore with later elements",
"            path.pop()                   # un-choose",
"    explore(0)",
"    return res"
]
},
{
"id": "dp-bridge",
"name": "Bridge to Dynamic Programming",
"cat": "core",
"clue": "Overlapping subproblems + optimal substructure → cache it",
"time": "O(n)",
"space": "O(n)",
"rec": "dp[i] = f(smaller dp[...])",
"mnemonic": "Backtracking that remembers = DP.",
"confus": [
"backtracking",
"fibonacci",
"dfs"
],
"template": [
"from functools import lru_cache",
"@lru_cache(maxsize=None)                 # memoization turns O(2^n) into O(n)",
"def fib(n):",
"    if n < 2:",
"        return n",
"    return fib(n - 1) + fib(n - 2)"
],
"example": null
},
{
"id": "fibonacci",
"name": "Fibonacci / 1-D DP",
"cat": "dp",
"clue": "Sequence where today = sum of the previous few",
"time": "O(n)",
"space": "O(1)",
"rec": "dp[i] = dp[i-1] + dp[i-2]",
"mnemonic": "Today = yesterday + the day before.",
"confus": [
"count-ways",
"kadane",
"dp-bridge"
],
"template": [
"def climb_stairs(n):",
"    a, b = 1, 1                          # ways to reach step 0 and 1",
"    for _ in range(2, n + 1):",
"        a, b = b, a + b                  # slide the window of two",
"    return b"
],
"example": null
},
{
"id": "kadane",
"name": "Kadane's Algorithm",
"cat": "dp",
"clue": "Maximum / minimum contiguous subarray run",
"time": "O(n)",
"space": "O(1)",
"rec": "cur = max(x, cur + x); best = max(best, cur)",
"mnemonic": "Carry the streak only if it helps.",
"confus": [
"sliding-window",
"prefix-sum",
"fibonacci"
],
"template": [
"def max_subarray(nums):",
"    cur = best = nums[0]",
"    for x in nums[1:]:",
"        cur = max(x, cur + x)            # extend or restart",
"        best = max(best, cur)",
"    return best"
],
"example": null
},
{
"id": "knapsack-01",
"name": "0/1 Knapsack",
"cat": "dp",
"clue": "Pick items under a capacity limit — each item once",
"time": "O(n·W)",
"space": "O(W)",
"rec": "dp[w] = max(dp[w], dp[w-wt] + val)  — capacity DOWNWARD",
"mnemonic": "Take it or leave it — each item once.",
"confus": [
"unbounded-knapsack",
"subset-sum",
"count-ways"
],
"template": [
"def knapsack_01(weights, values, W):",
"    dp = [0] * (W + 1)                   # dp[w] = best value with capacity w",
"    for i in range(len(weights)):",
"        for w in range(W, weights[i] - 1, -1):   # DOWNWARD -> item used once",
"            dp[w] = max(dp[w], dp[w - weights[i]] + values[i])",
"    return dp[W]"
],
"example": [
"def can_partition(nums):",
"    total = sum(nums)",
"    if total % 2:",
"        return False",
"    target = total // 2",
"    dp = [False] * (target + 1)          # dp[s] = can we reach sum s",
"    dp[0] = True",
"    for x in nums:",
"        for s in range(target, x - 1, -1):   # downward: each num once",
"            dp[s] = dp[s] or dp[s - x]",
"    return dp[target]"
]
},
{
"id": "unbounded-knapsack",
"name": "Unbounded Knapsack",
"cat": "dp",
"clue": "Pick items under a capacity limit — items reusable",
"time": "O(n·W)",
"space": "O(W)",
"rec": "dp[w] = max/min(dp[w], dp[w-wt] + val)  — capacity UPWARD",
"mnemonic": "Infinite supply — reuse freely.",
"confus": [
"knapsack-01",
"subset-sum",
"count-ways"
],
"template": [
"def coin_change(coins, amount):",
"    INF = float(\"inf\")",
"    dp = [0] + [INF] * amount            # dp[a] = fewest coins to make a",
"    for coin in coins:",
"        for a in range(coin, amount + 1):    # UPWARD -> reuse coin",
"            dp[a] = min(dp[a], dp[a - coin] + 1)",
"    return dp[amount] if dp[amount] != INF else -1"
],
"example": null
},
{
"id": "lcs",
"name": "Longest Common Subsequence",
"cat": "dp",
"clue": "Two strings — align them / longest shared subsequence",
"time": "O(n·m)",
"space": "O(n·m)",
"rec": "match: dp[i-1][j-1]+1; else max(dp[i-1][j], dp[i][j-1])",
"mnemonic": "Match → diagonal +1; mismatch → best of dropping one.",
"confus": [
"edit-distance",
"palindrome-subseq",
"lis"
],
"template": [
"def lcs(a, b):",
"    n, m = len(a), len(b)",
"    dp = [[0] * (m + 1) for _ in range(n + 1)]",
"    for i in range(1, n + 1):",
"        for j in range(1, m + 1):",
"            if a[i - 1] == b[j - 1]:",
"                dp[i][j] = dp[i - 1][j - 1] + 1",
"            else:",
"                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])",
"    return dp[n][m]"
],
"example": null
},
{
"id": "lis",
"name": "Longest Increasing Subsequence",
"cat": "dp",
"clue": "One sequence — longest strictly increasing run",
"time": "O(n log n)",
"space": "O(n)",
"rec": "patience: replace first tail >= x, else append",
"mnemonic": "Patience sorting — place each card on the leftmost taller pile.",
"confus": [
"lcs",
"binary-search",
"kadane"
],
"template": [
"import bisect",
"def length_of_lis(nums):",
"    tails = []                           # tails[k] = smallest possible tail of LIS length k+1",
"    for x in nums:",
"        i = bisect.bisect_left(tails, x) # first tail >= x",
"        if i == len(tails):",
"            tails.append(x)              # x extends the longest pile",
"        else:",
"            tails[i] = x                 # x improves an existing pile's tail",
"    return len(tails)"
],
"example": null
},
{
"id": "palindrome-subseq",
"name": "Palindromic Subsequence",
"cat": "dp",
"clue": "Palindrome problem over a substring i..j",
"time": "O(n²)",
"space": "O(n²)",
"rec": "ends ==: dp[i+1][j-1]+2; else max(dp[i+1][j], dp[i][j-1])",
"mnemonic": "Ends match → reach inward +2; else shrink a side.",
"confus": [
"lcs",
"edit-distance",
"matrix-chain"
],
"template": [
"def longest_palindrome_subseq(s):",
"    n = len(s)",
"    dp = [[0] * n for _ in range(n)]",
"    for i in range(n - 1, -1, -1):",
"        dp[i][i] = 1                     # single char is a palindrome",
"        for j in range(i + 1, n):",
"            if s[i] == s[j]:",
"                dp[i][j] = dp[i + 1][j - 1] + 2",
"            else:",
"                dp[i][j] = max(dp[i + 1][j], dp[i][j - 1])",
"    return dp[0][n - 1]"
],
"example": null
},
{
"id": "edit-distance",
"name": "Edit Distance",
"cat": "dp",
"clue": "Two strings — fewest insert/delete/replace to transform",
"time": "O(n·m)",
"space": "O(n·m)",
"rec": "match: dp[i-1][j-1]; else 1 + min(3 neighbours)",
"mnemonic": "Insert, Delete, Replace — pick the cheapest fix.",
"confus": [
"lcs",
"palindrome-subseq",
"string-partition"
],
"template": [
"def edit_distance(a, b):",
"    n, m = len(a), len(b)",
"    dp = [[0] * (m + 1) for _ in range(n + 1)]",
"    for i in range(n + 1):",
"        dp[i][0] = i                     # delete all",
"    for j in range(m + 1):",
"        dp[0][j] = j                     # insert all",
"    for i in range(1, n + 1):",
"        for j in range(1, m + 1):",
"            if a[i - 1] == b[j - 1]:",
"                dp[i][j] = dp[i - 1][j - 1]",
"            else:",
"                dp[i][j] = 1 + min(dp[i - 1][j - 1], dp[i - 1][j], dp[i][j - 1])",
"    return dp[n][m]"
],
"example": null
},
{
"id": "subset-sum",
"name": "Subset Sum",
"cat": "dp",
"clue": "Can a subset of numbers hit an exact target?",
"time": "O(n·target)",
"space": "O(target)",
"rec": "dp[s] = dp[s] or dp[s-x]",
"mnemonic": "Can these numbers hit the target?",
"confus": [
"knapsack-01",
"count-ways",
"unbounded-knapsack"
],
"template": [
"def subset_sum(nums, target):",
"    dp = [False] * (target + 1)          # dp[s] = is sum s achievable?",
"    dp[0] = True",
"    for x in nums:",
"        for s in range(target, x - 1, -1):",
"            dp[s] = dp[s] or dp[s - x]",
"    return dp[target]"
],
"example": [
"def find_target_sum_ways(nums, target):",
"    total = sum(nums)",
"    if (total + target) % 2 or abs(target) > total:",
"        return 0",
"    P = (total + target) // 2",
"    dp = [0] * (P + 1)                    # dp[s] = number of subsets summing to s",
"    dp[0] = 1",
"    for x in nums:",
"        for s in range(P, x - 1, -1):",
"            dp[s] += dp[s - x]",
"    return dp[P]"
]
},
{
"id": "string-partition",
"name": "String Partition",
"cat": "dp",
"clue": "Split a string into valid pieces (word break)",
"time": "O(n²)",
"space": "O(n)",
"rec": "dp[i] = any(dp[j] and s[j:i] valid)",
"mnemonic": "Cut the string where a piece is valid, recurse on the rest.",
"confus": [
"edit-distance",
"count-ways",
"matrix-chain"
],
"template": [
"def word_break(s, word_dict):",
"    words = set(word_dict)",
"    n = len(s)",
"    dp = [False] * (n + 1)               # dp[i] = s[:i] is segmentable",
"    dp[0] = True",
"    for i in range(1, n + 1):",
"        for j in range(i):",
"            if dp[j] and s[j:i] in words: # valid split point at j",
"                dp[i] = True",
"                break",
"    return dp[n]"
],
"example": null
},
{
"id": "catalan",
"name": "Catalan Numbers",
"cat": "dp",
"clue": "Count BSTs / balanced parenthesizations",
"time": "O(n²)",
"space": "O(n)",
"rec": "dp[n] = Σ dp[i-1]·dp[n-i]",
"mnemonic": "Pick a root/pivot, multiply left × right, sum over all pivots.",
"confus": [
"count-ways",
"fibonacci",
"matrix-chain"
],
"template": [
"def num_trees(n):",
"    dp = [0] * (n + 1)                   # dp[k] = #BSTs with k nodes",
"    dp[0] = dp[1] = 1",
"    for nodes in range(2, n + 1):",
"        for root in range(1, nodes + 1): # root splits into left/right sizes",
"            dp[nodes] += dp[root - 1] * dp[nodes - root]",
"    return dp[n]"
],
"example": null
},
{
"id": "matrix-chain",
"name": "Matrix Chain (Interval DP)",
"cat": "dp",
"clue": "Best split of an interval, combine the two halves",
"time": "O(n³)",
"space": "O(n²)",
"rec": "dp[i][j] = min over k (dp[i][k]+dp[k+1][j]+cost)",
"mnemonic": "Try every split point on the interval; combine the best.",
"confus": [
"palindrome-subseq",
"catalan",
"string-partition"
],
"template": [
"def max_coins(nums):",
"    balloons = [1] + nums + [1]          # padding for boundaries",
"    n = len(balloons)",
"    dp = [[0] * n for _ in range(n)]",
"    for length in range(2, n):           # interval length between the two pads",
"        for left in range(0, n - length):",
"            right = left + length",
"            for k in range(left + 1, right):     # k = last balloon burst in (left,right)",
"                dp[left][right] = max(",
"                    dp[left][right],",
"                    dp[left][k] + balloons[left] * balloons[k] * balloons[right] + dp[k][right]",
"                )",
"    return dp[0][n - 1]"
],
"example": null
},
{
"id": "count-ways",
"name": "Count Distinct Ways",
"cat": "dp",
"clue": "Count the NUMBER OF WAYS to reach a state",
"time": "O(n)",
"space": "O(1)",
"rec": "dp[i] += dp[i-1] (1-digit) + dp[i-2] (2-digit)",
"mnemonic": "Sum the ways from every state that can reach me.",
"confus": [
"fibonacci",
"subset-sum",
"catalan"
],
"template": [
"def num_decodings(s):",
"    if not s or s[0] == \"0\":",
"        return 0",
"    n = len(s)",
"    prev2, prev1 = 1, 1                   # dp[i-2], dp[i-1]",
"    for i in range(1, n):",
"        cur = 0",
"        if s[i] != \"0\":                  # single-digit decode",
"            cur += prev1",
"        if \"10\" <= s[i - 1:i + 1] <= \"26\":   # two-digit decode",
"            cur += prev2",
"        prev2, prev1 = prev1, cur",
"    return prev1"
],
"example": null
},
{
"id": "grid-dp",
"name": "DP on Grids",
"cat": "dp",
"clue": "Best path / cost on a 2D grid (right/down moves)",
"time": "O(m·n)",
"space": "O(n)",
"rec": "dp[r][c] = grid[r][c] + min/sum(dp[r-1][c], dp[r][c-1])",
"mnemonic": "Each cell sums/min's from its top and left neighbor.",
"confus": [
"matrix",
"lcs",
"edit-distance"
],
"template": [
"def min_path_sum(grid):",
"    cols = len(grid[0])",
"    dp = [float(\"inf\")] * cols",
"    dp[0] = 0",
"    for r in range(len(grid)):",
"        dp[0] += grid[r][0]              # first column: only from above",
"        for c in range(1, cols):",
"            dp[c] = grid[r][c] + min(dp[c], dp[c - 1])   # min(top, left)",
"    return dp[-1]"
],
"example": null
},
{
"id": "tree-dp",
"name": "DP on Trees",
"cat": "dp",
"clue": "Aggregate over a tree, children → parent (post-order)",
"time": "O(n)",
"space": "O(h)",
"rec": "rob = val + skip(children); skip = max(child states)",
"mnemonic": "Post-order: each node returns a small summary to its parent.",
"confus": [
"tree-traversal",
"dfs",
"graph-dp"
],
"template": [
"def rob(root):",
"    def dfs(node):",
"        if not node:",
"            return (0, 0)                # (rob this node, skip this node)",
"        l = dfs(node.left)",
"        r = dfs(node.right)",
"        rob_node = node.val + l[1] + r[1]    # rob node -> must skip children",
"        skip_node = max(l) + max(r)          # skip node -> children free to choose",
"        return (rob_node, skip_node)",
"    return max(dfs(root))"
],
"example": null
},
{
"id": "graph-dp",
"name": "DP on Graphs",
"cat": "dp",
"clue": "Optimal cost over edges with bounded steps",
"time": "O(K·E)",
"space": "O(V)",
"rec": "dist[v] = min(dist[v], snapshot[u] + w)",
"mnemonic": "Relax edges in a valid order; track best-to-reach.",
"confus": [
"dijkstra",
"bfs",
"tree-dp"
],
"template": [
"def find_cheapest_price(n, flights, src, dst, k):",
"    INF = float(\"inf\")",
"    dist = [INF] * n",
"    dist[src] = 0",
"    for _ in range(k + 1):               # at most k stops => k+1 edges",
"        snapshot = dist[:]               # use previous round only (Bellman-Ford bounded)",
"        for u, v, price in flights:",
"            if snapshot[u] + price < dist[v]:",
"                dist[v] = snapshot[u] + price",
"    return dist[dst] if dist[dst] != INF else -1"
],
"example": null
},
{
"id": "digit-dp",
"name": "Digit DP",
"cat": "dp",
"clue": "Count numbers in [0..N] with a digit property",
"time": "O(digits·states)",
"space": "O(digits·states)",
"rec": "dp(pos, tight, ...) → sum over digits 0..hi",
"mnemonic": "Build the number digit by digit, hugging the upper bound.",
"confus": [
"bitmask-dp",
"count-ways",
"probability-dp"
],
"template": [
"from functools import lru_cache",
"def count_unique_digits(n):",
"    digits = [int(c) for c in str(10 ** n - 1)]   # upper bound 10^n - 1",
"    L = len(digits)",
"    @lru_cache(maxsize=None)",
"    def dp(pos, mask, tight, started):",
"        if pos == L:",
"            return 1                     # one valid number formed",
"        total = 0",
"        hi = digits[pos] if tight else 9",
"        for d in range(0, hi + 1):",
"            if started and (mask >> d) & 1:",
"                continue                 # digit already used -> not unique",
"            new_started = started or d > 0",
"            new_mask = mask | (1 << d) if new_started else 0",
"            total += dp(pos + 1, new_mask, tight and d == hi, new_started)",
"        return total",
"    return dp(0, 0, True, False)"
],
"example": null
},
{
"id": "bitmask-dp",
"name": "Bitmask DP",
"cat": "dp",
"clue": "Small n (≤20) — the chosen SET is the state",
"time": "O(2^n·n)",
"space": "O(2^n)",
"rec": "dp[mask|bit][j] = min(dp[mask][i] + cost)",
"mnemonic": "The set of chosen items IS the state — store it in bits.",
"confus": [
"backtracking",
"digit-dp",
"subset-sum"
],
"template": [
"def shortest_tour(dist):",
"    n = len(dist)",
"    FULL = (1 << n) - 1",
"    INF = float(\"inf\")",
"    # dp[mask][i] = min cost to have visited 'mask' ending at city i",
"    dp = [[INF] * n for _ in range(1 << n)]",
"    dp[1][0] = 0                         # start at city 0",
"    for mask in range(1 << n):",
"        for i in range(n):",
"            if dp[mask][i] == INF or not (mask >> i) & 1:",
"                continue",
"            for j in range(n):",
"                if (mask >> j) & 1:",
"                    continue             # j already visited",
"                nmask = mask | (1 << j)",
"                dp[nmask][j] = min(dp[nmask][j], dp[mask][i] + dist[i][j])",
"    return min(dp[FULL][i] + dist[i][0] for i in range(n))   # return to start"
],
"example": null
},
{
"id": "probability-dp",
"name": "Probability DP",
"cat": "dp",
"clue": "Expected value / survival probability over steps",
"time": "O(states)",
"space": "O(states)",
"rec": "dp[state] = Σ parent_prob × transition_prob",
"mnemonic": "A state's probability = sum of (parent prob × transition prob).",
"confus": [
"count-ways",
"digit-dp",
"state-machine-dp"
],
"template": [
"def knight_probability(n, k, row, col):",
"    moves = [(1,2),(2,1),(2,-1),(1,-2),(-1,-2),(-2,-1),(-2,1),(-1,2)]",
"    dp = [[0.0] * n for _ in range(n)]",
"    dp[row][col] = 1.0",
"    for _ in range(k):",
"        nxt = [[0.0] * n for _ in range(n)]",
"        for r in range(n):",
"            for c in range(n):",
"                if dp[r][c]:",
"                    for dr, dc in moves:",
"                        nr, nc = r + dr, c + dc",
"                        if 0 <= nr < n and 0 <= nc < n:",
"                            nxt[nr][nc] += dp[r][c] / 8.0   # each move 1/8",
"        dp = nxt",
"    return sum(map(sum, dp))             # total prob still on the board"
],
"example": null
},
{
"id": "state-machine-dp",
"name": "State Machine DP",
"cat": "dp",
"clue": "A few discrete modes with transitions (buy/sell/hold)",
"time": "O(n)",
"space": "O(1)",
"rec": "sold=hold+p; hold=max(hold,rest-p); rest=max(rest,prev_sold)",
"mnemonic": "You're always in one of a few modes; transitions move you between them.",
"confus": [
"kadane",
"fibonacci",
"probability-dp"
],
"template": [
"def max_profit_cooldown(prices):",
"    if not prices:",
"        return 0",
"    hold = float(\"-inf\")                 # holding a stock",
"    sold = 0                             # just sold today (then cooldown)",
"    rest = 0                             # idle / past cooldown",
"    for p in prices:",
"        prev_sold = sold",
"        sold = hold + p                  # sell what we held",
"        hold = max(hold, rest - p)       # keep holding, or buy from rest",
"        rest = max(rest, prev_sold)      # stay resting, or come off cooldown",
"    return max(sold, rest)               # never end while holding"
],
"example": null
},
{
"id": "dijkstra",
"name": "Dijkstra's Algorithm",
"cat": "advanced",
"clue": "Shortest path — weighted graph, non-negative edges",
"time": "O((V+E) log V)",
"space": "O(V+E)",
"rec": "",
"mnemonic": "Always pick the cheapest unvisited node — greedy BFS with a price tag.",
"confus": [
"bfs",
"graph-dp",
"top-k"
],
"template": [
"import heapq",
"from collections import defaultdict",
"def dijkstra(graph, src, n):",
"    \"\"\"graph: adjacency list {u: [(v, weight), ...]}, n: number of nodes.\"\"\"",
"    dist = [float(\"inf\")] * n",
"    dist[src] = 0",
"    heap = [(0, src)]                    # (distance, node)",
"    while heap:",
"        d, u = heapq.heappop(heap)",
"        if d > dist[u]:",
"            continue                     # stale entry — skip",
"        for v, w in graph[u]:",
"            nd = d + w",
"            if nd < dist[v]:",
"                dist[v] = nd",
"                heapq.heappush(heap, (nd, v))",
"    return dist"
],
"example": [
"import heapq",
"from collections import defaultdict",
"def network_delay_time(times, n, k):",
"    graph = defaultdict(list)",
"    for u, v, w in times:",
"        graph[u].append((v, w))",
"    dist = {i: float(\"inf\") for i in range(1, n + 1)}",
"    dist[k] = 0",
"    heap = [(0, k)]",
"    while heap:",
"        d, u = heapq.heappop(heap)",
"        if d > dist[u]:",
"            continue",
"        for v, w in graph[u]:",
"            nd = d + w",
"            if nd < dist[v]:",
"                dist[v] = nd",
"                heapq.heappush(heap, (nd, v))",
"    mx = max(dist.values())",
"    return mx if mx < float(\"inf\") else -1"
]
},
{
"id": "union-find",
"name": "Union-Find (DSU)",
"cat": "advanced",
"clue": "Are X and Y connected? / merge groups dynamically",
"time": "O(α(n))",
"space": "O(n)",
"rec": "",
"mnemonic": "Every group has a boss; to merge, just change one boss.",
"confus": [
"dfs",
"bfs",
"graph-dp"
],
"template": [
"class UnionFind:",
"    def __init__(self, n):",
"        self.parent = list(range(n))",
"        self.rank = [0] * n",
"        self.components = n              # track number of groups",
"    def find(self, x):",
"        while self.parent[x] != x:",
"            self.parent[x] = self.parent[self.parent[x]]   # path compression (halving)",
"            x = self.parent[x]",
"        return x",
"    def union(self, a, b):",
"        ra, rb = self.find(a), self.find(b)",
"        if ra == rb:",
"            return False                 # already same group",
"        # union by rank: attach smaller tree under bigger",
"        if self.rank[ra] < self.rank[rb]:",
"            ra, rb = rb, ra",
"        self.parent[rb] = ra",
"        if self.rank[ra] == self.rank[rb]:",
"            self.rank[ra] += 1",
"        self.components -= 1",
"        return True",
"    def connected(self, a, b):",
"        return self.find(a) == self.find(b)"
],
"example": [
"def find_redundant_connection(edges):",
"    n = len(edges)",
"    uf = UnionFind(n + 1)               # nodes are 1-indexed",
"    for u, v in edges:",
"        if not uf.union(u, v):          # u and v already connected → this edge is redundant",
"            return [u, v]",
"    return []"
]
}
]
};
